#include "gtest/gtest.h"
#include "TouchpadInputEventSource.h"

TEST(TouchpadInputEventSourceTest, EventSourceCreates) { 
    int i = 1;
    ASSERT_EQ(i, 1);
}

TEST(TouchpadInputEventSourceTest, EventSourceCreates1) { 
    int i = 2;
    ASSERT_EQ(i, 2);
}

